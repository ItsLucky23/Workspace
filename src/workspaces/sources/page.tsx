//? Workspaces — Sources management (`/workspaces/sources`).

import Sources from '../_screens/Sources';

export const template = 'workspaces';

export default function SourcesPage() {
  return <Sources />;
}
