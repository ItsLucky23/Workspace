//? Workspaces — Account settings (`/workspaces/settings`).

import AccountSettings from '../_screens/AccountSettings';

export const template = 'workspaces';

export default function AccountSettingsPage() {
  return <AccountSettings />;
}
