//? Workspaces — Activity & event log (`/workspaces/activity`).

import Activity from '../_screens/Activity';

export const template = 'workspaces';

export default function ActivityPage() {
  return <Activity />;
}
