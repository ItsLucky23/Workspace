//? Workspaces — Pipeline editor (`/workspaces/pipeline`).

import Pipeline from '../_screens/Pipeline';

export const template = 'workspaces';

export default function PipelinePage() {
  return <Pipeline />;
}
