//? Workspaces — Terminals (`/workspaces/terminals`).

import Terminals from '../_screens/Terminals';

export const template = 'workspaces';

export default function TerminalsPage() {
  return <Terminals />;
}
