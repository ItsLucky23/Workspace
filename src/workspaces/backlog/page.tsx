//? Workspaces — Backlog & sprints (`/workspaces/backlog`).

import Backlog from '../_screens/Backlog';

export const template = 'workspaces';

export default function BacklogPage() {
  return <Backlog />;
}
